title: Diary Overview

columns:
  sidebar:
    sections:
      extends: sections/diary-entry
      files:
        type: files
  main:
    sections:
      fields:
        type: fields
        fields:
          text:
            type: textarea
            size: huge
